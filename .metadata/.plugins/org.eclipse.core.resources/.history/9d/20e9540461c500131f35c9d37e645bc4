import java.io.BufferedReader;
import java.io.IOException;
import java.io.InputStreamReader;
import java.util.ArrayList;
import java.util.Iterator;
import java.util.List;
import java.util.Scanner;


public class LuckyNumber {

	public static void main(String[] args) throws IOException {
		BufferedReader br = new BufferedReader(new InputStreamReader(System.in));
        String line = br.readLine();
        int N = Integer.parseInt(line);
        List<Integer> src = new ArrayList<Integer>();
        Scanner s = null;
        List<ArrayList<Integer>> luckdigits = new ArrayList<ArrayList<Integer>>();
        for (int i = 0; i < N; i++) {
        	 line = br.readLine();
        	 List<Integer> digits = new ArrayList<Integer>();
        	 s = new Scanner(line);
             while(s.hasNext()){
            	 digits.add(s.nextInt());
             }
             line = br.readLine();
             src.add(Integer.parseInt(line));
		}
        s.close();

	}
	
	public static List<String> getResults(List<ArrayList<Integer>> luckdigits, List<Integer> src ){
		List<String> results = new ArrayList<String>();
		for (Iterator iterator = src.iterator(); iterator.hasNext();) {
			Integer start = (Integer) iterator.next();
			
		}
		
		return results;
	}

}
