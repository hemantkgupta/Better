import java.io.BufferedReader;
import java.io.IOException;
import java.io.InputStreamReader;
import java.util.ArrayList;
import java.util.Iterator;
import java.util.List;
import java.util.Scanner;


public class LuckyNumber {

	public static void main(String[] args) throws IOException {
		BufferedReader br = new BufferedReader(new InputStreamReader(System.in));
        String line = br.readLine();
        int N = Integer.parseInt(line);
        List<Integer> src = new ArrayList<Integer>();
        Scanner s = null;
        List<ArrayList<Integer>> luckdigits = new ArrayList<ArrayList<Integer>>();
        for (int i = 0; i < N; i++) {
        	 line = br.readLine();
        	 List<Integer> digits = new ArrayList<Integer>();
        	 s = new Scanner(line);
             while(s.hasNext()){
            	 digits.add(s.nextInt());
             }
             line = br.readLine();
             src.add(Integer.parseInt(line));
		}
        s.close();

	}
	
	public static List<String> getResults(List<ArrayList<Integer>> luckdigits, List<Integer> src ){
		List<String> results = new ArrayList<String>();
		for (int i = 0; i < src.size(); i++) {
			List<Integer> digits = luckdigits.get(i);
			int start = src.get(i);
			if(digits.size() == 1 && digits.get(0)==0){
				results.add(i, "Impossible");
				break;
			}	
			if (start% 10 == 5 && digits.indexOf(5)== -1){
				results.add(i, "Impossible");
				break;
			}
			while(){
				
			}
			
			
		}
		
		public static int testLucky(List<Integer> digits, int start){
			int i = 1;
			while(true){
				String startString = Integer.toString(start);
				for (int j = 0; j < digits.size(); j++) {
					if(startString.indexOf(j)== -1){
						break;
					}
				}
				i++;
				start = start*;
			}
			
			return true;
		}
			

		
		return results;
	}

}
